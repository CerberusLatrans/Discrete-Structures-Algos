import math
from random import randrange

upper_range = 100
lower_range = 0
list_length = 21

list = []
for i in range(list_length):
    list.append(randrange(lower_range, upper_range))

print('Unsorted List', list)

def merge_sort(list):
    num_steps = len(list)/(2**)
    for i in range(num_steps)
    master_array = []
    for element in list:
        micro_list = [element]
        master_array.append(micro_list)
    print(int(len(list)/2))
    for i in range(int(len(list)/2)):
        pass
    return master_array

print('Sorted List:', merge_sort(list))